# Supabase — the app renders local sample data until these are set.
# Find these in your Supabase dashboard: Project Settings → API.
NEXT_PUBLIC_SUPABASE_URL="https://bpanebopdbmpxiguynnb.supabase.co"
# Publishable key (sb_publishable_...) — safe to expose to the browser. Read-only via RLS.
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=""
